# Media-Backlog-Analyzer
The premise is for a movie/TV show to be added to a list manually by a user as a backlog/catalogue for media to watch. The adding of an entry to the list would be a trigger, which will prompt a program to scrape the web for information such as length, genre, release year, etc. and then adding all that data as a record to a spreadsheet or db
